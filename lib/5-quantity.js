// Generated by CoffeeScript 1.6.3
(function() {
  var NEW, NUMBER, TRM, alert, badge, debug, echo, help, info, log, rpr, warn, whisper, π;

  TRM = require('coffeenode-trm');

  rpr = TRM.rpr.bind(TRM);

  badge = '﴾number﴿';

  log = TRM.get_logger('plain', badge);

  info = TRM.get_logger('info', badge);

  whisper = TRM.get_logger('whisper', badge);

  alert = TRM.get_logger('alert', badge);

  debug = TRM.get_logger('debug', badge);

  warn = TRM.get_logger('warn', badge);

  help = TRM.get_logger('help', badge);

  echo = TRM.echo.bind(TRM);

  π = require('coffeenode-packrattle');

  NUMBER = require('./4-number');

  NEW = require('./NEW');

  this.quantity = π.seq(this.NUMBER.literal, this.unit);

  this.TESTS = {
    'digits: parses sequences of ASCII digits': function(test) {
      var probe, _i, _len, _ref, _results;
      _ref = "0 12 7 1928374 080".split(/\s+/);
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        probe = _ref[_i];
        _results.push(test.eq(this.digits.run(probe), NEW.literal('digits', probe, probe)));
      }
      return _results;
    }
  };

}).call(this);
